import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    defaultText: {
      color: 'black',
      fontSize: 24,
      padding: 15,
      backgroundColor: "#00BFFF",
      textAlign: 'center',
    },
    buttonText: {
      fontSize: 24,
      textAlign: 'center',
  },
});

export {styles};